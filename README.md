# QingGeng Evaluator Example (flask version)

## Install

Install Flask by pip:

```
pip install Flask
```

Or if you use `pipenv`:

```
pipenv install
```

## Execute

```
FLASK_APP=evaluator.py flask run
```

or

```
FLASK_APP=evaluator.py pipenv run flask run
```
